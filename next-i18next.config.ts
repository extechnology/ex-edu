module.exports = {
  i18n: {
    locales: ["en", "fr", "es", "de"], // Supported languages
    defaultLocale: "en", // Default language
    localeDetection: true, // Auto-detect user's language
  },
};
